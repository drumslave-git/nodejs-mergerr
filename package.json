{
  "name": "media-merge-app",
  "version": "1.0.0",
  "description": "A simple Node.js web app to monitor a folder for multi‑part media and merge them on demand.",
  "main": "server.js",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "start": "node server.js",
    "docker:up": "docker-compose -f compose.yml up -d",
    "docker:down": "docker-compose -f compose.yml down",
    "docker:dev:up": "docker-compose -f compose.yml -f compose.dev.yml up -d",
    "docker:dev:down": "docker-compose -f compose.yml -f compose.dev.yml down"
  },
  "dependencies": {
    "dotenv": "^17.2.3",
    "react": "^19.2.3",
    "react-dom": "^19.2.3"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^5.1.2",
    "vite": "^7.3.0"
  }
}
